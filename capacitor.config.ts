import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.piter.marta.ola',
  appName: 'ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
